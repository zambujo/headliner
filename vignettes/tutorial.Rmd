---
title: "How to publish headlines"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{Using headliner to publish headlines}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup, eval=FALSE}
library(headliner)
library(dplyr)
library(stringr)
```

# Producing a simple feed

Let's see how publish `sciencegeist` RSS data for the latest month.

```{r, eval=FALSE}
data(sciencegeist)

sciencegeist %>% 
  filter(pubDate > "2020-06-01") %>%
  build(title = "Sciencegeist newest posts")

```

